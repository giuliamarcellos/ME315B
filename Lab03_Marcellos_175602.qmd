---
title: "Lab03"
author: "Giulia Marcellos"
format: html
editor: visual
---

```{r}
library(readr)
library(tidyverse)
library(dplyr)
```

1)  Importe, utilizando o pacote readr, cada um dos três arquivos disponíveis. Os objetos resultantes devem ser chamados flights, airlines e airports.

Para o arquivo de aeroportos, importe apenas as colunas IATA_CODE, CITY, STATE, LATITUDE, LONGITUDE;

```{r}
airports <- read_csv("airports.csv", col_select = c(IATA_CODE, CITY, STATE, LATITUDE, LONGITUDE))
head(airports)

```

Para o arquivo de vôos: a)Importe apenas as colunas DESTINATION_AIRPORT e ARRIVAL_DELAY; Leia apenas 1 milhão de linhas por vez;

```{r}
getStats <- function(x, pos) x

flights <- read_csv_chunked("flights.csv", callback = DataFrameCallback$new(getStats), chunk_size = 1000000, col_types = cols_only(DESTINATION_AIRPORT = col_character(), ARRIVAL_DELAY = col_double()))
```

Remova vôos em que o aeroporto de destino comece com a letra 1;

```{r}
flights <- flights %>% filter(!is.na(DESTINATION_AIRPORT), !is.na(ARRIVAL_DELAY))
```

Remova registros em que existam pelo menos uma coluna faltante;

```{r}
flights <- flights %>% drop_na()
```

Determine, para cada parte do arquivo, as estatísticas suficientes para a determinação do atraso médio por aeroporto de destino;

```{r}
getStats <- function(x, pos) x %>% filter(!startsWith(DESTINATION_AIRPORT, "1")) %>% drop_na() %>%  group_by(DESTINATION_AIRPORT) %>% summarise(n = n(), soma = sum(ARRIVAL_DELAY), .groups = "drop")

stats <- read_csv_chunked("flights.csv", callback = DataFrameCallback$new(getStats), chunk_size = 1000000, col_types = cols_only(DESTINATION_AIRPORT = col_character(), ARRIVAL_DELAY = col_double()))
```

Ao finalizar a leitura do arquivo, combine as estatísticas suficientes de modo a produzir a média de atraso por aeroporto.

```{r}
atraso <- stats %>% group_by(DESTINATION_AIRPORT) %>% summarise(n = sum(n), soma = sum(soma), .groups = "drop") %>% mutate(media = soma / n)
atraso
```

2)Selecione a operação apropriada join para incluir, na tabela flights, as colunas CITY e STATE do objeto airports. Para executar esta tarefa: Identifique a coluna que é a chave na tabela flights; Identifique a coluna que é a chave na tabela airports; Quais são os aeroportos que estão listados em flights, mas estão ausentes em airports? Quais são os aeroportos que estão listados em flights, mas estão ausentes em airports?

```{r}
flights %>% anti_join(airports, by = c("DESTINATION_AIRPORT" = "IATA_CODE")) %>% distinct(DESTINATION_AIRPORT)

flights1 <- flights %>% left_join(airports %>% select(IATA_CODE, CITY, STATE), by = c("DESTINATION_AIRPORT" = "IATA_CODE"))
flights1
```

3)Quantos aeroportos cada estado possui? Apresente uma tabela ordenada de forma decrescente (no número de aeroportos).

```{r}
aero_estado <- airports %>% group_by(STATE) %>% summarise(n_aeroportos = n(), .groups = "drop") %>% arrange(desc(n_aeroportos))
aero_estado
```

4)Apresente um mapa representando todos os atrasos observados por aeroporto.

Carregue o pacote leaflet; Combine os comandos leaflet, addTiles e addMarkers para a criação de um mapa básico; Armazene o grafico em b) numa variável chamada this; Adicione as duas linhas abaixo após a criação da variável this (apenas se você estiver usando Jupyter):

```{r}
library(leaflet)
mapa <- airports %>% filter(!is.na(LATITUDE), !is.na(LONGITUDE))
this <- leaflet(mapa) %>% addTiles() %>% addMarkers(lng = ~LONGITUDE, lat = ~LATITUDE)
this
```

```{r}
Sys.setenv(TZ = "America/Sao_Paulo")
Sys.time()
```

